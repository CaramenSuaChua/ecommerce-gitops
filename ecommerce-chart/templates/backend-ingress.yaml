apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ecommerce-backend-ingress
  namespace: ecommerce
spec:
  ingressClassName: nginx
  rules:
    - host: {{ .Values.backend.ingress.host }}
      http:
        paths:
          - backend:
              service:
                name: ecommerce-backend-service
                port:
                  number: {{ .Values.backend.ingress.port }}
            path: /
            pathType: Prefix